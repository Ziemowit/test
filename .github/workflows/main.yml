name: Kopytko

on:
  workflow_dispatch:
    inputs:
      commit:
        description: 'The branch, tag or SHA to checkout'
        required: true
        type: string
      tag:
        description: 'The optional docker image tag for the output target'
        required: false
        type: string

jobs:
  assign_tag:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      release_tag: ${{ steps.assign_tag.outputs.release_tag }}
    steps:
      - name: Set custom TAG
        if: "${{ inputs.tag != '' }}"
        run: echo "RELEASE_TAG=${{ inputs.tag }}" >> $GITHUB_ENV

      - name: Set current date as TAG
        if: "${{ inputs.tag == '' }}"
        run: echo "RELEASE_TAG=R.$(date +'%Y-%m-%dT%H-%M-%S')" >> $GITHUB_ENV

      - uses: actions/checkout@v4
      - run: |
          git tag ${{ env.RELEASE_TAG }}
          git push --tag
#
#      - name: Assign tag
#        id: assign_tag
#        run: echo "release_tag=${{ env.RELEASE_TAG }}" >> "$GITHUB_OUTPUT"
#
#  build_dev:
#    needs: assign_tag
#    runs-on: ubuntu-latest
#    env:
#      AWS_REGION: eu-central-1
#    steps:
#      - run: echo "🎉 The job was automatically triggered by a ${{ needs.assign_tag.outputs.release_tag }} event."
